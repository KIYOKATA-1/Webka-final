<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <% if (language === 'ru') { %>ДЛЯ НЕГО<% } else { %>FOR HIM<% } %>
    </title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&display=swap");
      @import url("https://fonts.googleapis.com/css2?family=Anta&family=Kdam+Thmor+Pro&display=swap");
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        margin: 0;
        padding: 1px;
        background: rgb(0, 14, 22);
        height: 100vh;
        overflow-x: hidden;
      }
      ::-webkit-scrollbar {
        width: 0;
      }
      header {
        text-align: center;
        position: relative;
        overflow: hidden;
        display: grid;
        grid-template-columns: 1fr;
        grid-template-rows: 500px;
        font-family: "M PLUS Rounded 1c", sans-serif;
      }

      header img {
        width: 100%;
        filter: brightness(40%);
        height: 100%;
        object-fit: cover;
      }

      .header_text {
        position: absolute;
        justify-content: center;
        top: 20vh;
        left: 41vw;
        color: #f2f2f2;
        font-family: "Anta", sans-serif;
        font-size: 64px;
        animation: fromTop 1.4s ease-out forwards;
      }
      .parfume_img {
        width: 270px;
        height: 330px;
        position: relative;
        filter: brightness(80%);
        z-index: 1;
        left: 3px;
      }

      .parfume_list {
        display: flex;
        flex-wrap: wrap;
        justify-content: flex-start;
        background: #131b24;
        margin: 0 auto;
        border: 2px solid snow;
        border-radius: 30px;
        width: 80%;
        padding-top: 20px;
        max-width: 100%;
        align-items: flex-end;
      }

      .parfume_list ul {
        list-style-type: none;
        padding: 0;
        display: flex;
        font-family: "Kdam Thmor Pro", sans-serif;
        flex-wrap: wrap;
        justify-content: space-evenly;
      }

      .parfume_details {
        max-width: 300px;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        justify-content: center;
        z-index: 2;
        backdrop-filter: blur(8px);
        margin-bottom: 20px;
      }

      .parfume_list li {
        display: flex;
        flex-direction: column;
        border-radius: 16px;
        margin-right: 10px;
        color: snow;
        border: 2px solid snow;
        margin-left: 20px;

        margin-bottom: 20px;
      }
      .parfumes {
        position: relative;
        top: 60px;
      }
      .action-buttons button {
        width: 100px;
        height: 30px;
        position: relative;
        justify-content: center;
        align-items: center;
        align-self: center;
        background: #bebebe;
        border: none;
        font-size: 14px;
        font-weight: 500;
        border-radius: 20px;
        margin-left: 5px;
        display: flex;
        margin-top: 20px;
      }
      @keyframes fromTop {
        from {
          transform: translateY(50%);
        }
        to {
          transform: translateY(0);
        }
      }
      @keyframes fade-in {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      .fade-in {
        animation: fade-in 2s ease-out;
      }
      .back a {
        text-decoration: none;
        color: snow;
        position: relative;
        justify-content: center;
        text-align: center;
        left: 40px;
      }
    </style>
  </head>
  <header class="fade-in">
    <img src="/img/vercace.jpg" alt="" />
    <h2 class="header_text">
      <% if (language === 'ru') { %>ДЛЯ НЕГО<% } else { %>FOR HIM<% } %>
    </h2>
  </header>
  <body>
    <section class="parfumes">
      <div class="parfume_list fade-in">
        <ul>
          <% for (const parfume of parfumes) { %>
          <li>
            <img
              class="parfume_img"
              src="/uploads/<%= parfume.image %>"
              alt="<%= parfume.name %> Image"
              onerror="console.error('Error loading image:', '<%= parfume.image %>')"
            />
            <div class="parfume_details">
              <strong>Name:</strong> <%= parfume.name %>
              <strong>Genre:</strong> <%= parfume.genre %>
              <strong>Price:</strong> <%= parfume.price %>&nbsp;$
              <div class="action-buttons">
                <button>BUY</button>
              </div>
            </div>
          </li>
          <% } %>
        </ul>
      </div>
    </section>
    <div class="back">
      <a href="/main"><i class="fa-solid fa-chevron-left"></i>&nbsp;BACK</a>
    </div>
  </body>
</html>
